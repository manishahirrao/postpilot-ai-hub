import React from 'react';

// ProtectedRoute: Restricts access to authenticated/verified users
const ProtectedRoute = ({ children }) => {
  // TODO: Implement auth check, redirect, loading spinner
  return children;
};

export default ProtectedRoute;
